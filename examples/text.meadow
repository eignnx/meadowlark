fn main() {
    !li $t0, 'X'; # comments?
    !sw [0x0001], $t0;
    !li $t0, '\n';
    !sw [0x0001], $t0;

    alias ptr => $t0;
    alias byte => $t1;

    !li ptr, "Hello, World!\n\0";
    !lbu byte, [ptr];
    while $t1 do {
        !sb [0x0001], byte;
        !addi ptr, ptr, 1;
        !lbu byte, [ptr];
    }

    !li $a0, "Test exn DEBUG_PUTS";
    !li $a1, 19;
    !exn 0x0003;

    !halt;
}

[[addr(2100)]]
fn blah() {
    !jr $ra;
}